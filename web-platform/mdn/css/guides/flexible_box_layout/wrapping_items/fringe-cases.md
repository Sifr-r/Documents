# Fringe Cases

## Warnings & Notes

> [!NOTE]
> Use Firefox for the example below as other common browsers treat `collapse` as `hidden`.

> [!NOTE]
> Use Firefox for the example below as other common browsers treat `collapse` as `hidden`.

