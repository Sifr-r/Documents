# Fringe Cases

## Warnings & Notes

> [!NOTE]
> If you want to test the accuracy of the _primary_ pointing device, use [`pointer`](/en-US/docs/Web/CSS/Reference/At-rules/@media/pointer) instead.

> [!NOTE]
> More than one value can match if the available devices have different characteristics, although `none` only matches when none of them are pointing devices.

