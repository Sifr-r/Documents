# Fringe Cases

## Warnings & Notes

> [!NOTE]
> You may notice we skipped some node types here. The `Node.ENTITY_REFERENCE_NODE` (5), `Node.ENTITY_NODE` (6), and `Node.NOTATION_NODE` (12) values are no longer used, while the `Node.DOCUMENT_FRAGMENT_NODE` (11) value will be introduced in [Building and updating the DOM tree](/en-US/docs/Web/API/Document_Object_Model/Building_and_updating_the_DOM_tree).

