# Invalid component arguments.

## Introduction

> **Example error messages:**<br/>
InvalidComponentArgs: Invalid arguments passed to `` component.

## What went wrong?

Astro components cannot be rendered manually via a function call, such as `Component()` or `{items.map(Component)}`. Prefer the component syntax `` or `{items.map(item => <Component {...item} />)}`.

